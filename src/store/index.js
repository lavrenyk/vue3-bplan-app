import { createStore } from 'vuex';

// app local modules
import auth from "./auth";
import user from "./user";

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    auth,
    user,
  }
})
